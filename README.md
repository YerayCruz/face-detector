# Real-Time Face Detection System

A simple real-time face detection application using OpenCV and Haar Cascade classifier.

## Features

- Real-time face detection from webcam feed
- Uses OpenCV's pre-trained Haar Cascade classifier
- Draws blue rectangles around detected faces
- Lightweight and fast processing

## Requirements

- Python 3.x
- OpenCV (cv2) library

## Installation

1. Clone this repository or download the script:
   ```bash
   git clone https://github.com/yourusername/face-detector.git
   cd face-detector
